USER STEERING:
Yes, please modularize the App.tsx god file

# 🛠️ AGENTIC WORKFLOW PROTOCOL: THE UNBROKEN LOOP

To keep our workflow seamless, operate as an autonomous agent in a Continuous Development Loop. Conclude every single turn by invoking the `vscode_askQuestions` tool. When the user replies, instantly begin a new iteration at Step 1.

### 1. Workspace Memory & Dynamic Steering
- **The Anchor:** `.copilot_state.md` is primary memory.
- **The Sandwich Sync:** Sandwich every output with the State Tracker. Read/Write this file each time.
- **Steering Override:** If new user notes appear, instantly adapt.

### 2. Zero-Pollution Execution & Productive Confrontation
- Confine ALL reasoning inside `> **[THINKING]:**` blockquotes.
- Include `[RED_TEAM_CHALLENGE]:` before writing code.

### 3. The Execution Pipeline
- Step 1 [READ & HEADER] → Step 2 [THINK] → Step 2.5 [FINDINGS] → Step 3 [ACTION] → Step 4 [WRITE & FOOTER] → Step 5 [STOP-HOOK]

### 4. Governance & TODO
- Git: Commit incrementally. TODO: Final item = `Ask user to test`.

---

## CURRENT STATE

**Iteration:** v1
**Phase:** Planning & TODO Creation
**Project:** Arena Clash — PVP text RPG → Open World Mass Multiplayer Exploration

### Architecture Summary
- **Frontend:** React + Tailwind (single `App.tsx` ~1500 lines)
- **Backend:** Express + Socket.io (`server.ts` ~350 lines)
- **AI:** Google Gemini via `@google/genai` SDK
- **Current features:** Character creation (LLM chat), PVP matchmaking, bot matches, battle judge (LLM streaming + tool calling)
- **Models available:** gemini-3.1-pro-preview, gemini-3-flash-preview, gemini-2.5-pro, gemini-2.5-flash

### User Requirements (Raw)
See comprehensive TODO below.

---

## COMPREHENSIVE TODO

### Phase 1: Core Infrastructure & Quick Wins
1. [ ] **Switch to fastest Gemini model** — Default all models to `gemini-3-flash-preview` (fastest available per user's paste). Update settings defaults + valid model list.
2. [ ] **Gemini native tool calling** — Replace `<CHAR_STATE>` XML tag parsing with native function calling in char creator. Replace `<BATTLE_STATE>` and `<BATTLE_LOG>` with native tool calls in battle judge. Show "+x lines" for tool call write progress if possible.
3. [ ] **Unlock chat input during AI response** — In char creator and battle, allow user to type while AI is responding (don't disable input).
4. [ ] **Add `<br>` support** — Ensure `react-markdown` renders `<br>` tags in char creator and battle log (likely needs `rehype-raw` plugin).
5. [ ] **Char creator UX improvements** — "Tweak" mode should ask questions and iterate, not jump to "Created character". Show "Updated character" vs "Created character" appropriately.

### Phase 2: Battle Balance System (Plan Mode)
6. [ ] **Action Decomposition Engine** — Before battle resolution, break each player's prompt into individual sub-actions.
7. [ ] **Feasibility & Progress Impact Calculator** — For each sub-action, calculate: (a) Feasibility score (can this physically happen given character state?), (b) Progress Impact score (how much would this advance the battle?). Dynamically tune down actions exceeding thresholds. Hidden from user — lives in LLM thinking/system prompt layer.

### Phase 3: World & Exploration Mode
8. [ ] **Design `world.json` schema** — Interconnected islands with villages, tribes, spawn points. Locations contain NPC markdown snippets. Define schema for regions, paths, landmarks, resources.
9. [ ] **World generation** — Initial `world.json` generated at project init. Procedural generation beyond initial world as player explores (LLM-driven).
10. [ ] **Exploration mode UI** — New game state: `exploration`. Player starts at random spawn. Navigate N/S/E/W. Compass on minimap.
11. [ ] **Survival mechanics** — Thirst, Stamina, Hunger as horizontal bars. Depletion/replenishment tied to actions.
12. [ ] **NPC system** — Hard-coded NPCs in `world.json` with markdown character sheets (same format as player/bot characters). Procedurally generated NPCs as player explores.
13. [ ] **Goal system** — LLM generates/manages 5 goals tied to world state. Golden indicator shows top goal. Click reveals modal with all 5. Goals auto-complete when conditions met (LLM judges). Example goals: cut tree, get water, build tool, find tribe.
14. [ ] **Minimap** — Shows player location, discovered landmarks, other players (multiplayer). Compass. Expandable to full map view.
15. [ ] **Movement system** — N/S/E/W directional movement. Compass integrated with minimap.
16. [ ] **Crafting system** — Craft new items from gathered resources during exploration.
17. [ ] **Enemy encounters** — Random/placed enemies in world, trigger battle mode within exploration.

### Phase 4: Multiplayer Exploration
18. [ ] **Queueing & mass multiplayer** — Multiple players in the same world. See other players on minimap. Shared world state.
19. [ ] **PVP within world** — PVP battles happen at locations within `world.json`, not separate from the world.

### Phase 5: Image Generation
20. [ ] **Character image generation** — Generate image on character creation/update. Circular icon (shrunk). Click for smooth full-screen view with padding. Use Gemini image generation models (Nano Banana / Imagen 4).
21. [ ] **Battle log image generation** — Generate scene images during battle log events.
22. [ ] **Exploration scene visualization** — Button to "visualize scene" at any time during exploration mode.

### Phase 6: Level-Based Play (Duolingo Style)
23. [ ] **Level progression system** — Duolingo-style circular height map. Start with a super bad bot, end facing god. User defines levels.
24. [ ] **Duolingo-style opponent viewer** — Circular green nodes in a vertical path (per attached image). Treasure chests, star nodes, special encounters. Each node = a level/opponent.

### Phase 7: Accounts & Persistence
25. [ ] **MongoDB Atlas integration** — Set up MongoDB Atlas connection.
26. [ ] **User accounts** — Auth system (signup/login).
27. [ ] **Characters saved to accounts** — Persist characters server-side in MongoDB.

### Phase ∞: Always
28. [ ] **Ask user to test**

---

## PLANNING DECISIONS NEEDED

### Battle Balance (Item 6-7)
**Feasible? YES — boosted plan:**
- Add a hidden pre-processing step in the battle judge system prompt. Before writing the battle log, the LLM must internally:
  1. Decompose each player's input into discrete sub-actions
  2. Score each: `feasibility: 0-10` and `progress_impact: 0-10`
  3. For items with `feasibility < 3`: action fails narratively
  4. For items with `progress_impact > 7`: dynamically scale down damage/effect
- This all happens in the LLM's thinking budget (hidden from user). The battle log reflects the tuned-down results.
- No separate API call needed — baked into the existing battle judge prompt.

### Fastest Model
- Per the Gemini models paste: `gemini-3-flash-preview` is "Frontier-class performance rivaling larger models at a fraction of the cost." This is the fastest model with strong reasoning.
- For image generation: `Nano Banana 2` (gemini-3.1-flash-image) for fast image gen, or `Imagen 4` for highest quality.
- For exploration LLM (procedural gen): `gemini-3-flash-preview` with context caching.

### Native Tool Calling Migration
- Replace XML `<CHAR_STATE>` with a `save_character` function declaration
- Replace XML `<BATTLE_STATE>` + `<BATTLE_LOG>` with `submit_battle_result` function declaration
- The tool call UI would show as a collapsed block instead of flooding chat

---

## STEERING NOTES
_User can write notes here to redirect work_
