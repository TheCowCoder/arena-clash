A: """ [FINDINGS] AHA! Slash Crack does Gemini on the server side only. Arena Clash spreads Gemini work across live browsers, so each device can independently hit the API for battle judging, bot prep, exploration, and image work. That means more concurrency and less centralized throttling pressure control. AHA! Slash Crack defaults both core models to gemini-2.5-flash. Arena Clash defaults battle resolution to App.tsx:131, which is gemini-2.5-pro, and also kicks off a per-turn battle image pipeline at App.tsx:4087. Slash Crack is simply running a lighter workload profile. AHA! Slash Crack is on a newer @google/genai and does not disable SDK retries. Arena Clash explicitly disables SDK auto-retries in ai.ts:96. That means Slash Crack can absorb some transient 503s invisibly inside the SDK, while Arena Clash surfaces every retry at the app layer. AHA! Slash Crack has a hard server-side single-flight guard for judge resolution and a terminal failure path that unlocks the turn if judging fails. Arena Clash only recently gained the re-entry guard; the bigger remaining structural gap is that when battle judging gives up after repeated retries, the client emits an error at App.tsx:2823 but does not send a terminal failure/reset signal back to the server. That can still leave the room stranded in an “awaiting turn resolution” state. AHA! Slash Crack has no battle image generation load at all. Arena Clash’s image path is not the direct cause of pre-resolution 503s, but it does increase total demand on the same Gemini budget over time, especially if both players are active across multiple turns. My conclusion: Slash Crack “doesn’t have these errors” because it stacks several advantages at once: server-owned AI calls, flash defaults, SDK-level auto retry, no image load, and a judge failure escape hatch. Arena Clash is still missing at least one of those escape hatches even after the re-entry fix. [UPDATED_FINDINGS] I walked gemini_docs.md by TOC like aisle markers and only pulled the shelves that matter for this bug class: Gemini API libraries, Models, Rate limits, Billing, and Troubleshooting guide. AHA! The docs confirm that rate limits are per project, not per API key in gemini_docs.md:115737. That means Arena Clash’s browser-side Gemini fan-out is the real structural problem: multiple live clients hammer the same project budget from the edge. Moving Gemini server-side is the right direction. AHA! The docs also say preview/experimental models have tighter limits in gemini_docs.md:115745, and that for 503 UNAVAILABLE Google explicitly recommends retrying or temporarily switching from Gemini 2.5 Pro to Gemini 2.5 Flash in gemini_docs.md:132559. So your “models are fine” direction is compatible with keeping the chosen model as primary while still using a runtime fallback only when the service is overloaded. AHA! Slash Crack is structurally calmer for three reasons at once: server-owned Gemini calls, gemini-2.5-flash defaults, and SDK-managed retries in a newer @google/genai. Arena Clash currently does the opposite on the most failure-sensitive path: browser-owned calls, heavier battle load at App.tsx:131, manual retry surfacing via ai.ts:96, and extra post-turn image traffic at App.tsx:4087. AHA! The biggest remaining Arena Clash gap is not “more retries,” it is missing terminal failure handling after retry exhaustion. Right now the client can give up and emit a visible error at App.tsx:2823, but there is still no proper server-owned battle-resolution authority that can always clear or complete the room. Slash Crack has that escape hatch; Arena Clash still needs it. AHA! Your priorities line up with the doc guidance: keep image generation, keep model choice user-facing, but move Gemini orchestration to the server and upgrade the integration so request pressure is serialized and recoverable. [UPDATED_PLAN] Move Arena Clash Gemini calls server-side, using Slash Crack as the reference architecture. Start with the battle judge path first, because it is the user-visible blocker and the most sensitive to repeated 503s. Upgrade Arena Clash’s Gemini integration to a current SDK and re-evaluate where SDK retries vs app retries should live. Add a real server-owned terminal failure/reset path so a room cannot remain stranded after retry exhaustion. Keep image generation, but make it server-owned and backgrounded so it never competes with turn resolution on the client. Keep user-selected models, but add doc-aligned overload fallback behavior for 503 UNAVAILABLE instead of pretending repeated Pro retries will always recover. """ Please audit the above. Make a new todo labelling things as [done], [partial], or [missing] Finish all partial and missing and follow up with ask

[AUDIT TODO]
- [done] Move Arena Clash Gemini calls server-side. All browser Gemini requests now go through server proxy endpoints, and the leftover client-hosted battle judge block was removed from App.tsx.
- [done] Start with the battle judge path first. Battle resolution is now owned by server.ts via runBattleTurnResolution().
- [done] Upgrade Gemini integration to a current SDK and re-evaluate retries. @google/genai is now ^1.50.0, and retries are intentionally controlled in app/server code rather than stacked in the SDK.
- [done] Add a real server-owned terminal failure/reset path. Battle resolution now enters a shared 2-minute retry window and exposes a room-wide Retry Battle For 2m escape hatch instead of stranding the room.
- [done] Keep image generation, but make it server-owned and backgrounded. Image generation now uses the server proxy, and battle images remain post-resolution/backgrounded.
- [done] Keep user-selected models and add doc-aligned overload fallback behavior. Server retry logic now temporarily falls back from Pro to Flash on 503/UNAVAILABLE while preserving the selected primary model.

[FINAL AUDIT]
- No [partial] items remain.
- No [missing] items remain.
